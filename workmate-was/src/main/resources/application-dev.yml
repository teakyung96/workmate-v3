# 개발 서버 환경 — 접속 정보는 서버 환경변수로 주입
server:
    port: 8081

spring:
    datasource:
        url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${POSTGRES_DB}
        username: ${POSTGRES_USER}
        password: ${POSTGRES_PASSWORD}
    jpa:
        show-sql: true
